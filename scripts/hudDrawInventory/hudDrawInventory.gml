function hudDrawInventory(){
	
	draw_sprite_stretched(imgScreenInventory, 0, 0, 0, room_width, room_height);
	
	/*
	var eggs = 0;
	if(pc.eventTrigger[Event.palace1Clear]){ eggs ++; }
	if(pc.eventTrigger[Event.palace2Clear]){ eggs ++; }
	if(pc.eventTrigger[Event.palace3Clear]){ eggs ++; }
	if(pc.eventTrigger[Event.palace4Clear]){ eggs ++; }
	if(pc.eventTrigger[Event.palace5Clear]){ eggs ++; }
	if(pc.eventTrigger[Event.palace6Clear]){ eggs ++; }
	if(pc.eventTrigger[Event.palace7Clear]){ eggs ++; }
	if(pc.eventTrigger[Event.palace8Clear]){ eggs ++; }
	if(pc.eventTrigger[Event.palace9Clear]){ eggs ++; }
	*/
	
	

}